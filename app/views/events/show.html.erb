<% content_for :metatags do %>
  <meta property="robots" content="noindex, nofollow"/>
<% end %>

<% content_for :title do %>
  <%= "Festival de Inverno | " + @event.name %>
<% end %>

<% content_for(:side_bar) do %>
    <%= render :partial => "shared/sidebar_admin" %>
<% end %>

<div class="container_admin">
  <div class="content-body" >

    <div id="news_show">
      <div class="name">
        <%= @event.name%>
      </div>
      <div class="local">
        <%= @event.location %>
      </div>
      <div class="custo">
        <%= @event.cost %>
      </div>
      <div class="description">
        <%= @event.description.html_safe %>
      </div>
      <% if !@event.categories.empty?%>
        <% @event.category_ids.each do |cat|%>
          <div class="category">
      	    <%= @event.categories.find(cat).name%>
          </div>
        <%end%>
      <%end%>
    </div>
   

    <% if user_signed_in? %>
      <%= link_to "Editar", edit_event_path(@event), class: "waves-effect waves-light btn" %>
      <%= link_to "Deletar", events_path(@event), class: "waves-effect waves-light btn", method: :delete, data: {confirm: "Tem certeza desta Ação?"} %>
    <% end %>
  </div>
</div>

