<% content_for :metatags do %>
  <meta property="robots" content="noindex, nofollow"/>
<% end %>

<% content_for :title do %>
  <%= "Festival de Inverno | " + truncate(@event.title, length: 160) %>
<% end %>

<% content_for :description do %>
  <%= truncate(@event.description, length: 300) %>
<% end %>

<% content_for(:side_bar) do %>
    <%= render :partial => "shared/sidebar_admin" %>
<% end %>

<div class="container_admin">
  <div class="content-body" >

    <div id="news_show">
      <h5 class="title" >
        <%= @event.title %> 
      </h5>

      <div class="description">
        <%= @event.short_description.html_safe %>
      </div>         
      <div class="description">
        <%= @event.description.html_safe %>
      </div>
      <div class="row">
        <div class="att col-md-12 left">
          <img src="<%= image_url('category_4.png')%>" style="width:20px"/>
          <span class="description"><b>Duração: </b> <%= @event.duration %></span>

        </div>
        <div class="att col-md-12 left">
          <img src="<%= image_url('category_4.png')%>" style="width:20px"/>
          <span class="description"><b>Local:</b> <%= @event.address %></span>
        </div>  
        <div class="att col-md-12 left">
          <img src="<%= image_url('category_4.png')%>" style="width:20px"/>
          <span class="description"><b>Valor:</b> R$<%= @event.value %></span>
        </div>
        <div class="att col-md-12 left">
          <img src="<%= image_url('category_4.png')%>" style="width:20px"/>
          <span class="description"><b>Datas:</b> <%= @event.dates %></span>
        </div> 
          
      </div>
    </div>
   

    <% if user_signed_in? %>
      <%= link_to "Editar", edit_event_path(@event), class: "waves-effect waves-light btn" %>
      <%= link_to "Deletar", event_path(@event), class: "waves-effect waves-light btn", method: :delete, data: {confirm: "Tem certeza desta Ação?"} %>
    <% end %>
  </div>
</div>

